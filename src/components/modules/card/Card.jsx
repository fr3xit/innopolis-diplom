import styles from './Card.module.scss';

const Card = function () {
	return (
		<div className="card">
			<header></header>
			<main></main>
			<footer></footer>
		</div>
	);
};

export default Card;
